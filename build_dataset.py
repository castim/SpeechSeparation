from librispeech_mixer import LibriSpeechMixer

#Create the LibriSpeech mixer
#mixer = TestMixer(dataset_built=False)

mixer = LibriSpeechMixer(dataset_built=False, nbSamples=100)
#validation_mixer = LibriSpeechMixer(train=False, nbSamples=100, dataset_built=False)
